import "package:flutter/material.dart";

class WordItem extends StatelessWidget {
  const WordItem({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
